---
// v2 侧栏：手绘字标 + 频道导航 + 分面 chips；中段滚动，语言/主题钉底
import { ui } from "../i18n";
interface Props {
  locale: string;
  currentCat?: string | null;
  showFacets?: boolean;
  channelItems?: any[];
  tags?: any[];
  total: number;
  categories: any[];
}
const { locale, currentCat = null, showFacets = false, channelItems = [], tags = [], total, categories } = Astro.props;
const t = ui[locale as keyof typeof ui];
const FACETS = ["function", "stack", "style", "scenario"] as const;
const countIn = (id: string) => channelItems.filter((i: any) => i.tags.includes(id)).length;
---
<aside class="sidebar">
    <a class="brand" href={`/${locale}/`} aria-label="uicurio — home">
      <svg class="wordmark" viewBox="0 0 96 34" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M6.5 15.5 C6 20, 7 24.5, 9.5 25.2 C11.5 25.7, 13.5 20.5, 14 15.5"></path>
        <path d="M14 15.5 C14 20, 14.5 24, 16.5 24.8 C18.2 25.3, 19.6 23.4, 20.6 21.4"></path>
        <path d="M24.5 16.5 C24 20, 24 23.5, 24.7 25.5"></path>
        <path d="M26.6 10.6 L26.9 10.3"></path>
        <path d="M38.5 15.8 C34 14.5, 30.5 17.5, 30.3 21 C30.1 24.5, 33.5 26.8, 38.5 25.6"></path>
        <path d="M43.5 15.5 C43 20, 44 24.5, 46.5 25.2 C48.5 25.7, 50.5 20.5, 51 15.5"></path>
        <path d="M51 15.5 C51 20, 51.5 24, 53.5 24.8 C55.3 25.4, 57.5 23, 58.6 20.4"></path>
        <path d="M63 26.5 C62.5 22, 62.3 18, 62.8 14.8"></path>
        <path d="M62.8 18.5 C64.5 16.6, 67.6 16.1, 69.4 17.2"></path>
        <path d="M74 16.5 C73.6 20, 73.6 23.5, 74.3 25.5"></path>
        <path d="M76.1 10.6 L76.4 10.3"></path>
        <path d="M86.2 15.7 C83 15.4, 80 17.7, 80 20.8 C80 24.1, 82.9 26.3, 85.8 25.7 C88.8 25.1, 90.4 22.4, 89.9 19.7 C89.4 17.1, 87.1 15.6, 84.9 16.3"></path>
      </svg>
      <span class="brand-sub">{t.tagline}</span>
    </a>
  <div class="side-scroll">
    <div class="side-block">
      <p class="side-label">{t.channels}</p>
      <nav class="navlist" aria-label={t.channels}>
        <a class="navrow" href={`/${locale}/`} aria-current={currentCat === null ? "page" : undefined}>
          <i class="dot" aria-hidden="true"></i><span>{t.all}</span><span class="count">{total}</span>
        </a>
        {categories.map((c: any) => (
          <a class="navrow" href={`/${locale}/collections/${c.slug}/`} aria-current={currentCat === c.slug ? "page" : undefined}>
            <i class="dot" aria-hidden="true"></i><span>{c.name[locale]}</span><span class="count">{c.count}</span>
          </a>
        ))}
      </nav>
    </div>
    {showFacets && (FACETS as readonly string[]).map((facet) => (
      <div class="facet-group" data-facet={facet}>
        <p class="side-label">{t[facet as "function" | "stack" | "style" | "scenario"]}</p>
        <div class="chiprow">
          {tags.filter((x: any) => x.facet === facet).map((x: any) => (
            <button type="button" class="chip" data-tag={x.id} aria-pressed="false">
              <span class="t">{x.name[locale]}</span>
              <span class="n">{countIn(x.id)}</span>
            </button>
          ))}
        </div>
      </div>
    ))}
  </div>
  <div class="side-foot">
    <select class="langsel" data-lang-select aria-label="Language">
      <option value="en" selected={locale === "en"}>English</option>
      <option value="zh" selected={locale === "zh"}>中文</option>
    </select>
    <button type="button" class="iconbtn" data-theme-toggle aria-label={t.theme}>
      <svg class="i-sun" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2m0 16v2M4.9 4.9l1.4 1.4m11.4 11.4 1.4 1.4M2 12h2m16 0h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"></path></svg>
      <svg class="i-moon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z"></path></svg>
    </button>
  </div>
</aside>
